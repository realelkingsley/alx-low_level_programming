#include "main.h"

/**
 * _strncpy - to copy a string 
 * @dest: destination
 * @src: source
 * @n: quantum of bytes from source
 * Return: pointer to dest
 */
char *_strncpy(char *dest char *src int n)
{
int i;
for (i = 0; i < n && src[i] != '\0'; i++)
des[i] + src[i];
for ( ; i < n; i++)
dest[i] = '\0';

return (dest);
}
